
Функция ОбработчикВызоваWebhook(Запрос)
	
	Ответ = Новый HTTPСервисОтвет(200);
	
	Если Запрос.ПараметрыURL["Ключ"] = Константы.ТГ_WebhookToken.Получить() Тогда
		
		//ТГ_бот.ОбновитьЗапросы();
		ТГ_бот.ОбработатьЗапросы();
		
		Ответ.УстановитьТелоИзСтроки("ok");
		
	КонецЕсли;
	
	Возврат Ответ;
	
КонецФункции

Функция Любойpost(Запрос)
	
	Ответ = Новый HTTPСервисОтвет(200);
	Возврат Ответ;
	
КонецФункции
