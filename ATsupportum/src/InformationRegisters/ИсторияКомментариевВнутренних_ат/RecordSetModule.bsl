
Процедура ПередЗаписью(Отказ, Замещение)
	
	Для каждого Запись Из ЭтотОбъект Цикл
		
		Запись.ТекстКомментария = РаботаСHTML_КлиентСервер_ат.ПолучитьОбычныйТекстИзHTML(Запись.КомментарийВHTML);
		
	КонецЦикла;
	
КонецПроцедуры
